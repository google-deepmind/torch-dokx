language: erlang

env:
  - LUA="th"

branches:
  only:
    - master

install:
  - echo "==== Installing gfortran ===="
  - sudo apt-get install gfortran
  - echo "==== Installing virtualenv ===="
  - sudo pip install virtualenv
  - echo "==== Installing dependencies ===="
  - curl -skL https://raw.github.com/torch/ezinstall/master/install-deps | sudo bash &> /dev/null
  - echo "==== Installing Torch ===="
  - curl -skL https://raw.github.com/torch/ezinstall/master/install-luajit+torch | sudo bash &> /dev/null
  - echo "==== Building torch-dokx, using luarocks ===="
  - sudo /usr/local/bin/luarocks make
  - echo "==== Building torch-dokx with documentation, using doxk-luarocks ===="
  - sudo /usr/local/bin/dokx-luarocks make

script: 'for x in tests/test*.lua ; do echo "==== $x ====" ; th $x ; done'

notifications:
  recipients:
    - danhgn@gmail.com
  email:
    on_success: change
    on_failure: always
